# Dependencies for data generation, model training, and the Jupyter notebook.
# (The backend has its own minimal requirements.txt in backend/)

pandas==2.2.3
numpy==2.1.3
matplotlib==3.9.2
seaborn==0.13.2
scikit-learn==1.8.0
joblib==1.5.3
jupyter==1.1.1

# Optional - the training script and notebook automatically include XGBoost
# as an extra model if it's installed:
# xgboost==2.1.1
